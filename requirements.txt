Django>=5.0,<6.0
Pillow>=10.0

# --- Deployment dependencies (Vercel / any production host) ---
whitenoise>=6.6          # serves static files without a separate CDN/server
dj-database-url>=2.1     # parses DATABASE_URL into Django's DATABASES setting
psycopg[binary]>=3.1     # Postgres driver, used when DATABASE_URL points at Postgres
django-storages>=1.14    # S3-compatible media storage (only used if AWS_* env vars are set)
boto3>=1.34               # required by django-storages' S3 backend
gunicorn